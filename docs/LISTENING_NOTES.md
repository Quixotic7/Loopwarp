# Listening Notes

No formal listening pass has been captured yet.

Initial listening targets:

- `tape`: transparent at 1x, useful varispeed offset
- `tempo_varispeed`: loop fits declared beats without hard reset
- `chopped`: clean gate at conservative settings, controlled stutter at extremes
- `granular`: stable neutral texture, bounded spray/jitter
- `random_ola`: subtle movement at low macro, bounded chaos at high macro
- `pitch_corrected`: approximate pitch restoration with distinct `PitchShift` color
